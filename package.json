{
  "name": "otomatpro",
  "version": "2.0.0",
  "private": true,
  "description": "OtomatPro — Supabase + Netlify",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "install": "npm i --prefix frontend",
    "build": "npm i --prefix frontend && npm run build --prefix frontend"
  }
}
